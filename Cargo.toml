[package]
name = "releaser"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.18", features = ["derive"] }
regex = "1.10.6"
serde_json = { version = "1.0", features = ["preserve_order"] }
